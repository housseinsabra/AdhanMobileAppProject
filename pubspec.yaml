name: adhan
description: A Muslim prayer time application using a public API.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0 # Needed for API calls
  intl: ^0.19.0 # Needed for DateFormat

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0 # Use the latest version

flutter:
  uses-material-design: true